---
description: "撰写和审查用户故事地图：规则 + 检查清单"
---

# 用户故事地图撰写技能

## 触发条件

需要撰写或审查产品需求文档中的用户故事地图时。

## 核心规则

### 结构三层

```
活动（Activity）→ 任务（Task）→ 故事（Story）
```

- **活动**：用户旅程的宏观阶段（按用户行为划分，不是系统模块）
- **任务**：活动内的具体动作（用户做什么）
- **故事**：以"用户要…"开头的用户视角描述

### 格式规范

1. **标题**：`# 量潮[产品名]用户故事地图`
2. **用户画像**：一行描述目标用户（"用户画像：需要…的[角色]"）
3. **活动**：每个活动有描述段（说明流程/目标/价值），然后是任务列表
4. **任务**：`### 任务：[动词+名词]`，下面是故事列表
5. **故事**：`· 用户要[动作]，[目的/价值]`

### 故事格式

```
· 用户要[动作]，[目的/价值]
```

正确示例：
- "用户要登记一个新产品，写明名称、状态、负责人"
- "用户要每天写下情绪日记，系统识别认知扭曲"

错误示例（系统动作）：
- "系统要记录部署状态"（应改为"用户要记录部署状态…"）
- "支持多租户"（应改为"用户要按租户隔离数据…"）

### 活动组织

- 按**用户旅程时间顺序**（用户先做什么→后做什么）
- 不按**系统模块分类**（"认证/存储/分析"是模块，不是活动）

## 检查清单

撰写完成后逐条检查：

- [ ] 每个活动都有描述段（一段话说明流程/目标/价值）
- [ ] 所有故事都是"用户要…"句式（没有系统动作混入）
- [ ] 活动按用户旅程时间顺序排列（不是系统架构顺序）
- [ ] 每个活动下的任务按用户动作逻辑排列
- [ ] 故事颗粒度一致（没有一条过大或过细）
- [ ] 没有功能清单式描述（"支持XX功能"不是故事）
- [ ] 用户画像一行完成（角色 + 需求）

## 反模式

| 问题 | 示例 | 修正 |
|------|------|------|
| 系统动作 | "系统要记录部署状态" | "用户要记录部署状态…" |
| 功能清单 | "支持多租户、支持审计" | "用户要按租户隔离数据…" |
| 模块当活动 | "活动：认证模块" | "活动：用户登录" |
| 缺描述段 | 直接列任务 | 先写一段活动流程说明 |
| 颗粒度不一致 | 一条过大（"管理整个系统"）/过细（"点击保存按钮"） | 统一在"用户要XX"粒度 |

## 参考文档

- 标准范本：`qtcloud-product/requirement.md`
- 活动描述段写法：见范本"活动：需求"下的流程说明
